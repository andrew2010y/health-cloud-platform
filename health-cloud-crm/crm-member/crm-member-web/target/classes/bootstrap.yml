spring:
  application:
    name: crm-member-web

eureka:
  instance:
    non-secure-port: ${server.port:80}
    preferIpAddress: true
  client:
    service-url:
      defaultZone: http\://localhost\:8761/eureka/